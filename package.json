{
  "name": "pnpm-test",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=16.0.0"
  },
  "scripts": {
    "lint": "eslint foo.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "eslint-config-custom": "workspace:*"
  }
}
